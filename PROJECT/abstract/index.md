# Abstract

GstreamerExp is a real-time GStreamer video pipeline experiment testbed. It studies controlled VP8/RTP/UDP behavior through declarative specs, preserved runs, local runners, generated reports, and a project-local static viewer.

Entelesis manages the project as a workspace project. It links and validates the project, but the local runner and experiment substrate stay inside the GstreamerExp repository.
