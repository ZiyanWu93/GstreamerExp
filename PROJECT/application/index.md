# Application

The application surface is project-local. The static viewer and generated reports make experiments browsable, while the CLI and experiment runner operate the actual pipeline work.

Entelesis Project Management should show the project declaration, operation targets, validation checks, and roadmap status. It should not embed the local website as the project profile.
