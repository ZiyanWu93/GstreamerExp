name: fixed-5mbps-400ms
description: 'Static impairment via tc on $NIC at the sender: 5 Mbps, 400 ms one-way

  delay, no loss. Single step that holds for the run. One of the RTT-boundary

  sweep networks (H9): used to find the base delay at which SCReAM''s

  delay-based control degenerates. NIC set via network_env: { NIC: <ifname> }.

  '
camera_steps:
- duration: 0
  rate_kbps: 5000
  delay_ms: 400
  loss:
    model: none
  label: "5 Mbps \xB7 400 ms"
viewer_steps: []
