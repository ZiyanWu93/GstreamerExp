name: fixed-5mbps-20ms-loss2pct
description: 'Static impairment via tc on $NIC: 5 Mbps, 20 ms delay, 2% uniform iid

  packet loss. Single step that holds for the run. One of the loss-boundary

  sweep networks (H10): find the loss rate at which SCReAM''s delivered quality

  degenerates (recovery off, so loss is unmasked). NIC via network_env: { NIC }.

  '
camera_steps:
- duration: 0
  rate_kbps: 5000
  delay_ms: 20
  loss:
    model: uniform
    pct: 2
  label: "5 Mbps \xB7 20 ms \xB7 2% loss"
viewer_steps: []
