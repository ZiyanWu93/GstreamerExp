meta:
  name: distributed_scream_norecovery_budget0_fluc
  description: 'Distributed (aum→veda) SCReAM at 720p30 over

    `fluctuating-5mbps-300kbps`. Recovery: none (no retransmission).

    Latency budget: 0 ms (no enforcement). One arm of the H4 fluctuating-network

    experiment — same 2×2×2 (CC × retransmission × budget) as the

    constant-network version, but on a more realistic

    capacity-step network.

    '
  flags:
  - sudo
  - scream
scenario:
  actors:
    camera: {}
    viewer: {}
  setup_delay_seconds: 3.0
  drain_delay_seconds: 1.0
  metrics:
  - frame_count
  - encoder_target_kbps
  - frame_latency
  - wire_bytes
  - encoded_bitrate
  - decoder_errors
  - late_drops
sync:
  mode: shared_epoch
  termination: all
streams:
- name: main
  priority: 0
  video: ball-720p30
  codec: vp8
  encoder:
    bitrate_kbps: 1500
    keyframe_interval_frames: 60
  congestion_control:
    algorithm: scream
    init_bitrate_kbps: 1500
    min_bitrate_kbps: 200
    max_bitrate_kbps: 4000
  sink:
    backend: fake
    sync: false
  recovery:
    nack: false
    pli: false
    fec: false
  latency_budget_ms: 0
  network: fluctuating-5mbps-300kbps
